{{#if siteSettings.communitarian_enabled}}
  {{#if canCreateResolution}}
    {{#if category}}
      {{d-button
        class=btnClass
        id="create-resolution"
        action=(action "clickCreateResolutionButton")
        icon="plus"
        disabled=disabled
        label="communitarian.resolution.new_button"
      }}
    {{/if}}
  {{/if}}
{{/if}}
