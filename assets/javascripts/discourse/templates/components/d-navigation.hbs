{{#if showCategoryAdmin}}
  {{#if isCommunitiesPage}}
    {{new-community-button canCreateCommunity=true}}
  {{else}}
    {{categories-admin-dropdown
      onChange=(action "selectCategoryAdminDropdownAction")
      options=(hash
        triggerOnChangeOnTab=false
      )
    }}
  {{/if}}
{{/if}}
